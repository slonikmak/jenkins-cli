{
  "name": "jk-cli",
  "version": "1.0.0",
  "description": "Jenkins CLI utility for checking build status and failure reasons",
  "main": "bin/index.js",
  "type": "module",
  "bin": {
    "jk": "./bin/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.5.2",
    "commander": "^12.1.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
